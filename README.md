# ukify-scipts
Scripting for automatic UKIfication of the kernel, initrd, and cmdline on ubuntu.

There is no installation script. Just copy whatever you find useful to the path obtained by strapping the project’s
path.